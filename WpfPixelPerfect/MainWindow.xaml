<Window x:Class="WpfPixelPerfect.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WpfPixelPerfect"
        SizeToContent="WidthAndHeight"
        MinWidth="400"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None" WindowStartupLocation="CenterScreen" Focusable="True">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Name="controlPanel" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" BorderThickness="1" CornerRadius="8 8 0 0" BorderBrush="Gray" Background="GhostWhite" Width="400" HorizontalAlignment="Center">
            <Grid Margin="8 8 8 4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" >
                    <Button Name="loadButton" Content="Load Image"  VerticalAlignment="Center"  />
                    <Slider Name="opacitySlider" Width="150" Minimum="0.1" Maximum="1.0" Value="0.4" Margin="10 0 0 0" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding Path=Value, ElementName=opacitySlider, StringFormat={}{0:0.0}}" Margin="5 0 0 0" VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" FlowDirection="RightToLeft">
                    <Button Name="close" Content="Close" Margin="10 0 0 0" />
                </StackPanel>
            </Grid>
        </Border>
        
        <Image Grid.Row="1" Name="screen" Opacity="{Binding Path=Value, ElementName=opacitySlider}" Stretch="None" VerticalAlignment="Top" HorizontalAlignment="Left"/>      
    </Grid>
</Window>
